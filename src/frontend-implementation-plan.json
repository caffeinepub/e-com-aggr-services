{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Implement minimal arbitrage module with computation and logging",
  "requirements": [
    {
      "id": "REQ-28",
      "summary": "Create arbitrage computation module with set_arbitrage function",
      "acceptanceCriteria": [
        "Function accepts two numeric parameters p1 and p2",
        "Computes p3 as p1 minus p2",
        "Returns object with properties { p1, p2, p3 }",
        "Function is exported from the module"
      ],
      "file_operations": [
        {
          "path": "frontend/src/lib/arbitrage.ts",
          "operation": "modify",
          "description": "Update the existing set_arbitrage function to accept two numeric parameters p1 and p2, compute p3 = p1 - p2, and return an object { p1, p2, p3 }. Ensure the function is exported from the module."
        }
      ]
    },
    {
      "id": "REQ-29",
      "summary": "Add get_arbitrage function with console logging and future integration TODO",
      "acceptanceCriteria": [
        "Function accepts two numeric parameters p1 and p2",
        "Internally calls set_arbitrage(p1, p2) to compute result",
        "Logs the { p1, p2, p3 } object to console.log",
        "Includes TODO comment mentioning future payout integration and Merkle-root integration",
        "Function is exported from the module"
      ],
      "file_operations": [
        {
          "path": "frontend/src/lib/arbitrage.ts",
          "operation": "modify",
          "description": "Update the existing get_arbitrage function to accept two numeric parameters p1 and p2, internally call set_arbitrage(p1, p2), log the resulting { p1, p2, p3 } object to console.log, and include a TODO comment for future payout and Merkle-root integration. Ensure the function is exported from the module."
        }
      ]
    },
    {
      "id": "REQ-30",
      "summary": "Integrate arbitrage computation into order creation flow",
      "acceptanceCriteria": [
        "Import get_arbitrage function from frontend/src/lib/arbitrage.ts into Products.tsx",
        "After computing order total in the mock order creation handler, calculate mockWholesalePrice as total * 0.8",
        "Call get_arbitrage(total, mockWholesalePrice) to compute and log arbitrage values",
        "No money is sent and no external service integration occurs",
        "Arbitrage computation happens before or after the order API call without blocking order creation"
      ],
      "file_operations": [
        {
          "path": "frontend/src/pages/Products.tsx",
          "operation": "modify",
          "description": "Import get_arbitrage from frontend/src/lib/arbitrage.ts. In the existing mock order creation handler, after computing the order total, calculate mockWholesalePrice as total * 0.8 and call get_arbitrage(total, mockWholesalePrice). Ensure this computation does not block order creation and does not send money or integrate with external payment services."
        }
      ]
    }
  ]
}